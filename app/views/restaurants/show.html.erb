<h1>Show Page</h1>
<h3> <%= @restaurant.name %> </h3>
<p>Address: <%= @restaurant.address%> <br>
  It's a <%= @restaurant.category %> restaurant.</p>
  <ul>
    Reviews:
  <% @restaurant.reviews.each do |review| %>
    <p><%= review.content %>.</p>
    <p>Rating: <%= review.rating %></p>
  <% end %>

  </ul>

<%= link_to "Review this restaurant", new_restaurant_review_path(@restaurant) %> <br>
<%= link_to "Back to list", restaurants_path %>
